#include <stdio.h>
#include <stdbool.h>
bool isPrime(intx)
{
  if(x<2)
{
  return false
}
  bool isPrime = true;
 for(int i = 2; i<=x /2; i++)
}
 if(x% i ==0)
{
 if(x% i ==0)
{
 prime = false;
}
}
  int main()
{
  int x;
 scanf("%d", &x);
 if(isPrime(x))
{
 printf("");
}
 else
{
 printf("Not prime");
}
 return 0;
}
